import type { TypeCheckResult } from './typescript/runTypeCheck';
export declare function verifyTypeScriptSetup({ dir, distDir, distDirRoot, cacheDir, strictRouteTypes, tsconfigPath, typeCheckPreflight, disableStaticImages, hasAppDir, hasPagesDir, appDir, pagesDir, debugBuildPaths, }: {
    dir: string;
    distDir: string;
    /** The root dist directory without /dev suffix, used for fixed type paths */
    distDirRoot?: string;
    cacheDir?: string;
    strictRouteTypes: boolean;
    tsconfigPath: string | undefined;
    typeCheckPreflight: boolean;
    disableStaticImages: boolean;
    hasAppDir: boolean;
    hasPagesDir: boolean;
    appDir?: string;
    pagesDir?: string;
    debugBuildPaths?: {
        app?: string[];
        pages?: string[];
    };
}): Promise<{
    result?: TypeCheckResult;
    version: string | null;
}>;
